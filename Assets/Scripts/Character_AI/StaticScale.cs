using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StaticScale : MonoBehaviour
{
   /* Vector3 oldScale = new Vector3(1, 1, 1);
    void Update()
    {
        Vector3 parentScale = this.transform.parent.transform.localScale;
        Vector3 lastScale = new Vector3(parentScale.x / oldScale.x, parentScale.y / oldScale.y, parentScale.z / oldScale.z);
        if(oldScale != parentScale)
        transform.localScale = new Vector3(transform.localScale.x / lastScale.x, transform.localScale.y / lastScale.y, transform.localScale.z / lastScale.z);
        oldScale = parentScale;
    }   */
}
